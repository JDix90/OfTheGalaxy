# Dependencies
node_modules/
# package-lock.json is committed for reproducible `npm ci` installs in CI.
yarn.lock
pnpm-lock.yaml

# Environment variables (only .env.example may be committed)
.env
.env.*
!.env.example

# Build outputs
dist/
build/
*.log

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Testing
coverage/
.nyc_output/

# Misc
*.bak
*.tmp

# Archives / handoff bundles (may embed .env files with secrets — never commit)
*.zip
zimkEb7A
