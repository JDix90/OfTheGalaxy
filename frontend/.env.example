# API Configuration
# Dev: leave relative (/api) so requests go same-origin through the Vite proxy and
# work on any dev port. Production / split-host: set an absolute URL (e.g.
# https://api.example.com/api).
VITE_API_URL=/api
